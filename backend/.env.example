# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration
# JWT (JSON Web Token) is used to keep users logged in securely.
# This secret is used to sign tokens - make it long and random!
# Generate one at: https://generate-secret.vercel.app/64
JWT_SECRET=your-secure-jwt-secret-minimum-32-characters-long
JWT_EXPIRES_IN=7d

# Gmail API OAuth
# To set up Gmail API OAuth:
# 1. Go to https://console.cloud.google.com/
# 2. Create a project and enable Gmail API
# 3. Create OAuth credentials (Desktop app)
# 4. Run: node src/services/gmail-setup.js <client_id> <client_secret>
# 5. Follow the prompts and paste the values below
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REFRESH_TOKEN=your-google-refresh-token
EMAIL_FROM=promptstackhello@gmail.com

# Frontend URL (for email links and CORS)
FRONTEND_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX=5
